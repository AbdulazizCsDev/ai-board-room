# --- AI Board Room: dependencies (pinned for reproducible deploys) ---

langchain==1.2.15                 # core chains / prompt + LLM plumbing
langchain-core==1.3.0
langchain-openai==1.1.15          # OpenAI chat model + embeddings
langchain-community==0.4.1        # document loaders, retrievers
langchain-text-splitters==1.1.2   # RAG chunking
openai==2.26.0
tiktoken==0.12.0
pydantic==2.9.0                   # the shared data schema (the contract)
pydantic-settings==2.14.0         # load config from env / .env
python-dotenv==1.0.1
faiss-cpu==1.13.2                 # local vector store (RAG)
pypdf==6.10.2                     # PDF loader
fastapi==0.115.0                  # REST API framework
uvicorn[standard]==0.30.6         # ASGI server
python-multipart==0.0.12          # multipart/form-data (file uploads)
aiofiles==24.1.0                  # required by FastAPI StaticFiles
